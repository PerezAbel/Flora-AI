# Base URL for the inference API (FastAPI server in ../Server)
EXPO_PUBLIC_INFERENCE_URL=http://localhost:8000

# If your API needs auth, uncomment and set:
# EXPO_PUBLIC_INFERENCE_TOKEN=your-token-here
