import SectionScreen from '@/components/section-screen';

export default function RecommendationsTab() {
  return (
    <SectionScreen
      title="Feedback"
      body="Actionable agent recommendations based on current conditions, trends, and prior interventions."
    />
  );
}
