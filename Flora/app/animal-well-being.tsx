import SectionScreen from '@/components/section-screen';

export default function AnimalWellBeingScreen() {
  return (
    <SectionScreen
      title="Agent Animal Well Being"
      body="Monitor livestock health notes, environment warnings, and care recommendations."
    />
  );
}
