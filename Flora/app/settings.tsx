import SectionScreen from '@/components/section-screen';

export default function SettingsScreen() {
  return (
    <SectionScreen
      title="Agent Settings"
      body="Configure detection sensitivity, notification channels, and preferred intervention style."
    />
  );
}
